<% include ../partials/header %>
<body>

    <!-- Following Menu -->

    <!-- Page Contents -->
    <div class="pusher">

        <div class="ui vertical stripe segment">
            <div class="ui middle aligned stackable grid container">
                <div class="row">
                    <div class="two wide column">
                        <h3 class="ui header">All Links </h3>

                    </div>
                    <div class="four wide column right">
                        <a href="/urls/new" class="ui basic button">
                          <i class="icon user"></i>
                          Add Link
                        </a>
                    </div>
                  </div>
                  <div class="row">
                    <table class="ui celled table" id="example">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Original URL</th>
                                <th>Shorted</th>
                                <th>Click Count</th>
                                <th>Action</th>
                            </tr>
                        </thead>

                        <tbody>

                            <% users.forEach(function(user){  %>
                                <tr>
                                    <td>
                                        <%= user.id %>
                                    </td>
                                    <td>
                                        <a href="<%= user.url %>"><%= user.url %></a>
                                    </td>
                                    <td>
                                      <a href="/user/<%= user.shorted%>"><%= user.shorted%></a>
                                      
                                    </td>
                                    <td>
                                      <%= user.click %>
                                    </td>

                                    <td>
                                        <div class="ui icon buttons">
                                            <a href="/urls/<%= user.id %>" class="ui button"><i class="unhide icon"></i></a>
                                            <a href="/urls/<%= user.id %>/edit" class="ui button"><i class="edit icon"></i></a>
                                            <a href="/urls/<%= user.id %>/delete" onclick="return confirm('Are you sure you want to delete this item?');" class="ui button"><i class="trash icon"></i></a>
                                        </div>
                                    </td>
                                </tr>
                                <% }); %>
                        </tbody>

                    </table>
                </div>
            </div>
        </div>

    </div>
    <% include ../partials/footer %>

</body>

</html>
